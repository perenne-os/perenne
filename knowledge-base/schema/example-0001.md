# Schema example

This file used to hold an inline copy of a filled record. To avoid two diverging
copies of the same issue (the kind of reference drift the knowledge base exists
to prevent), the worked example now lives as a single source of truth:

- **Filled, real example:** [`../entries/KB-0001.md`](../entries/KB-0001.md) —
  "Git SSH-signed commits fail with passphrase prompt on Windows".
- **Blank template + field-by-field schema:** [`issue-record.md`](issue-record.md).

Read `KB-0001.md` to see every field populated against a genuine problem we hit
during Phase 0.
