# Brainstorming / visual-companion scratch files
.superpowers/

# Local Claude Code settings (may contain personal info)
.claude/

# Rust / Cargo build output
/target/
**/target/
# Cargo.lock is committed (Phase 3c): an audited crypto dependency entered
# the tree, so the exact resolved versions must be reproducible.

# Editor / OS noise
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
