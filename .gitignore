# Local AI/agent tool scratch & settings (may contain personal info).
# These are whatever coding assistant a contributor happens to use; never
# committed. Add your own tool's local dir here as needed.
.superpowers/
.claude/
.aider*
.cursor/

# Rust / Cargo build output
/target/
**/target/
# Cargo.lock is committed (Phase 3c): an audited crypto dependency entered
# the tree, so the exact resolved versions must be reproducible.

# Editor / OS noise
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
